import Project from '../item/Project'
import View from './View'

test('number()', () => {
    /*
    const project = new Project()
    const view = new View(project)
    console.log(view._handleMouseEvent('click', null, null))
    */
    // expect(formatter.number(22.325)).toStrictEqual(22.33)
})
